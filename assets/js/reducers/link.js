import Immutable from 'immutable';
import actionTypes from '../actions/actionTypes';

export const link = (state = Immutable.Map(), { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
